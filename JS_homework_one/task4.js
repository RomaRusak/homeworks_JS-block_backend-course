import { a7 as diametr } from './task2.js';

const task4 = () => {
  const VCilindra = Math.PI * Math.pow(diametr / 2, 2) * 10;
  console.log(`площадь цилиндра: ${VCilindra.toFixed(4)} м3`);
};

export default task4;
